## 1. i18n 基础设施搭建

- [x] 1.1 安装并接入 `vue-i18n`，在应用入口注册全局 i18n 实例
- [x] 1.2 建立翻译资源目录与首批语言文件（`zh-CN`、`en-US`），定义 key 命名规范
- [x] 1.3 实现 locale 解析优先级（用户设置 > 系统偏好 > 默认值）并在启动前生效

## 2. 语言设置与持久化

- [x] 2.1 扩展 `SettingsModel` 与默认设置，新增 `locale` 字段及类型约束
- [x] 2.2 在设置页提供语言切换入口，并将选择结果持久化到 settings store
- [x] 2.3 确保重启后恢复用户语言，且首次启动可按系统语言自动选取受支持 locale

## 3. 核心壳层与导航文案迁移

- [x] 3.1 迁移路由 meta 与导航配置文案（含 `page-nav`、settings 导航）到翻译键
- [x] 3.2 迁移 `App`、`Sidebar`、`Header`、全局弹窗与命令面板文案到 i18n
- [x] 3.3 迁移全局空态、占位、按钮文本与高频 Toast 文案到 i18n

## 4. 页面与组件文案迁移

- [x] 4.1 迁移 Workspace/Project/Task/Inspector 相关页面与组件的用户可见文案
- [x] 4.2 迁移 Assets（Snippets/Vault/Notes/Diary）与 Review/Trash/Settings 文案
- [x] 4.3 执行硬编码文案扫描并清理核心路径中的遗留字符串

## 5. 时间与错误本地化统一

- [ ] 5.1 封装统一日期/时间/相对时间工具，替换分散 `zh-CN` 与直写 locale 逻辑
- [ ] 5.2 建立后端错误码到 i18n key 的映射表，并接入 `ApiError` 展示链路
- [ ] 5.3 为未映射错误实现 message/通用文案兜底，保证异常场景可读

## 6. Tauri 托盘文案本地化

- [ ] 6.1 在 Tauri 启动阶段读取生效 locale 并映射托盘菜单文案
- [ ] 6.2 明确运行时语言变更下托盘策略（即时更新或重启生效）并实现对应行为
- [ ] 6.3 校验托盘“显示主窗口/退出应用”在双语下文案正确

## 7. 验收与发布准备

- [ ] 7.1 执行构建与类型检查，修复 i18n 接入导致的类型与导入问题
- [ ] 7.2 按页面清单进行中英文回归验证（导航、弹窗、Toast、时间、错误）
- [ ] 7.3 补充国际化约定文档（key 规范、新增文案流程、回退策略）
